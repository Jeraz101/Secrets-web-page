
//Things to do for jeff_eye_project
/*create .gitignore
check the gitignore in secrets, copy and paste in gitignore of jeff_eye_project
learn about git push again before you push your files.
learn github and version control ebfore deploying your app.

check how to deploy to heroku, servers that have gitignore
*/

hashing password
npm install md5

ok remove mongoose encryption


level 4
Salting and hashing password.
install nvm in your folder
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.38.0/install.sh | bash

after running it close your terminal and open it aagin, then check your version. nvm --version

install node
nvm install 14.17.0

cd over to your directory and npm install bcrypt

app.js
const bcrypt = require("bcrypt"); //
const saltRounds = 10;




app.post("/register", function(req, res){

  bcrypt.hash(req.body.password, saltRounds, function(err, hash){
    const newUser = new User({
      email: req.body.username,
      password:hash                    // md5(req.body.password) //password hashing / check to see if this is connected in our jeff_eye_project
    });

    newUser.save(function(err){
      if(err){
        console.log(err);
      }else{
        res.render("secrets");
      }
    })
  })


})

installing express and passport. Using passport.js to ADD cookies and sessions
https://www.npmjs.com/package/express-session
Passport
npm i passport passport-local passport-local-mongoose express-session

loggin in using facebook or gmail: http://www.passportjs.org/packages/
click on passport google 2.0
click on google developer console
write your project name
click on create

Remeber when your website is alive you need to comeback to credentials-> oauth client id to change your "Authorized javascript origins url " to your url website

https://console.cloud.google.com/apis/credentials/oauthclient?previousPage=%2Fapis%2Fcredentials%3Fproject%3Dsecret-314113&project=secret-314113
